projection;
strict ( 2 );

define behavior for ZC_SO_Header //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association Items { create; }
}

define behavior for ZC_SO_ITEM //alias <alias_name>
{
  use update;
  use delete;

  use association _Header;
}